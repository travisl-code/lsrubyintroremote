NoMethodError: undefined method `keys' for Array

See sample .rb file for exercise where error is replicated.
This error means that you're trying to call the .keys method
on an array, which does not use key-value pairs.
An array is simply an ordered, indexed list.